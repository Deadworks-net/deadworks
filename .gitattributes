# Everything under docker/ runs inside a Linux container. A CRLF checkout (core.autocrlf on
# Windows) would make the image fail at start with "bad interpreter: /usr/bin/env bash^M".
docker/** text eol=lf
